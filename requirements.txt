# Python deps for the video pipeline (scripts/retarget_mediapipe.py).
# Install in a separate venv from Isaac Lab — mediapipe pins protobuf and
# numpy ranges that clash with Isaac Lab's environment.
#
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt

mediapipe>=0.10.18
opencv-python>=4.9
numpy>=1.24,<2.0
scipy>=1.11
yt-dlp>=2024.10.0
